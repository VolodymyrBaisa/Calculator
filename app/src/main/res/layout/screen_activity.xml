<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <data>
        <variable
            name="grandTotalSign"
            type="ua.bios.mvvm.viewmodel.GrandTotalViewModel" />

        <variable
            name="taxRateSign"
            type="ua.bios.mvvm.viewmodel.TaxRateViewModel" />

        <variable
            name="memorySign"
            type="ua.bios.mvvm.viewmodel.MemoryViewModel" />

        <variable
            name="calculatorScreenHandler"
            type="ua.bios.mvvm.viewmodel.CalculatorScreenHandler" />
    </data>

    <LinearLayout
        style="@style/layout"
        android:layout_width="match_parent"
        android:layout_height="wrap_content">

        <FrameLayout
            android:layout_width="match_parent"
            android:layout_height="match_parent">

            <LinearLayout
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:orientation="horizontal">

            <TextView
                android:id="@+id/gtSign"
                style="@style/sign"
                android:layout_width="20dip"
                android:layout_height="match_parent"
                android:text="@{grandTotalSign.GTActivate ? @string/app_gt_sign : ``}" />

            <TextView
                android:id="@+id/rateSign"
                style="@style/sign"
                android:layout_width="wrap_content"
                android:layout_height="match_parent"
                android:text="@{@string/app_tax_rate_sign + `(` + taxRateSign.value +`)`}" />

                <TextView
                    android:id="@+id/memSign"
                    style="@style/sign"
                    android:layout_width="wrap_content"
                    android:layout_height="match_parent"
                    android:text="@{memorySign.memoryActivate ? @string/app_memory_sign + ` ` + memorySign.getSize : ``}" />
            </LinearLayout>

            <ua.bios.display.CalculatorScreen
                android:id="@+id/screen"
                style="@style/screen"
                android:layout_width="match_parent"
                android:layout_height="match_parent"
                android:onTextChanged="@{calculatorScreenHandler.onTextChanged}"
                app:setFont="@{`fonts/Calculator.otf`}" />
        </FrameLayout>
    </LinearLayout>
</layout>